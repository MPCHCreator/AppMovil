<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="domoticSystem.Login">

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Image Grid.Row="0" Source="usuario.png" WidthRequest="100" HeightRequest="100" Margin="0,50,0,30"/>

            <StackLayout Grid.Row="1" Padding="30,20">
                <Editor x:Name="Mensaje1" Placeholder="usuario" PlaceholderColor="White"/>
                <Editor x:Name="Mensaje2" Placeholder="contraseña" PlaceholderColor="White"/>

                <Button Text="Iniciar sesion" VerticalOptions="Center" HorizontalOptions="Center" BackgroundColor="WhiteSmoke" TextColor="#141420" Clicked="Button_Clicked"/>
            </StackLayout>
        </Grid>

    </Grid>

</ContentPage>
